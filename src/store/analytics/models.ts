// FIXME: it would be great if we could type the event data without using context
export enum AnalyticsEvent {
  signUp = 'signUp',
  signIn = 'signIn',
  resetPassword = 'resetPassword',
  signOut = 'signOut',
  navigate = 'navigate',
}
